# Required environment variables
# Do NOT commit your real keys. Use .env.local for local development.

# Server-side (Vercel Serverless Functions)
# Recommended for production since the key should never be exposed to the browser.
ANTHROPIC_API_KEY=

# Optional client-side (NOT recommended)
# If you ever call the provider directly from the browser (not recommended), Vite only exposes vars prefixed with VITE_.
VITE_CLAUDE_API_KEY=
